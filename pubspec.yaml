name: josh_frank_resume
description: "A Resume Website for Joshua Frank"
publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 1.0.0+1

environment:
  sdk: '>=3.4.3 <4.0.0'

dependencies:
  flame: ^1.21.0
  flame_forge2d: ^0.18.2+1
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^3.0.0

flutter:

  uses-material-design: true

  assets:
    - assets/images/parallax/
    - assets/images/


  